package com.yubiniperez.kinalapp.repository;

import com.yubiniperez.kinalapp.entity.DetalleVenta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CetalleVentaRepository extends JpaRepository<DetalleVenta, Long> {
}
